(*) start
----------------
 npm i mongoose express dotenv cors jsonwebtoken 

(*) package.json
---------------------
   "scripts": {
    "start": "nodemon index.js"
  },


(*)jwt authentication
-----------------------
npm install jsonwebtoken

const jwt = require("jsonwebtoken");  // Import jwt library

